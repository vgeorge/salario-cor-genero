import colors from "./colors";

const config = {
  colors: colors,
  debounceTime: 100,
  outerBox: {
    margin: 20,
    padding: {
      top: 10,
      left: 30
    }
  },
  chart: {
    xBuffer: 50
  }
};

export default config;
