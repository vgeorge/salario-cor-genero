import colors from "./colors";

const config = {
  colors: colors,
  debounceTime: 100,
  outerBox: {
    margin: 20,
    padding: {
      top: 10,
      left: 30
    }
  },
  chart: {
    xBuffer: 50,
    barStroke: 2,
    margin: { top: 10, right: 20, bottom: 80, left: 50 }
  }
};

export default config;
