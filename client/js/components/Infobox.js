import React, { Component } from "react";
import { render } from "react-dom";

class Infobox extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div>
        <p>Busque uma profissão:</p>
      </div>
    );
  }
}

export default Infobox;
